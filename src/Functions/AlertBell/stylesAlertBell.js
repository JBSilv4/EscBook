import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  boxAlert: {
    backgroundColor: '#5100cc',
    position: 'absolute',
    top: -5,
    left: 25,
    width: 30,
    height: 18,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10
  },
  value: {
    color: '#fff',
    fontWeight: 'bold',
    fontSize: 10
  }
});

export default styles